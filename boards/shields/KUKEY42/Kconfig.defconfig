if SHIELD_KUKEY42_L || SHIELD_KUKEY42_R
    config ZMK_KEYBOARD_NAME
        default "KUKEY42"

    config NFCT_PINS_AS_GPIOS
        default y

    config ZMK_SPLIT
        default y

    config ZMK_POINTING
        default y
endif
